name: Deploy Frontend to S3 + CloudFront

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # Upgraded to v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4 # Upgraded to v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Sync frontend to S3
      run: |
        aws s3 sync ./frontend s3://${{ secrets.S3_BUCKET }} --delete

    - name: Invalidate CloudFront cache
      # This check ensures the step only runs if the secret is actually provided
      if: ${{ secrets.CLOUDFRONT_DIST_ID != '' }}
      run: |
        aws cloudfront create-invalidation \
          --distribution-id ${{ secrets.CLOUDFRONT_DIST_ID }} \
          --paths "/*"